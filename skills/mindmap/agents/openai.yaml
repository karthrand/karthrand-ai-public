interface:
  display_name: "Mindmap"
  short_description: "把自然语言整理成 markmap 思维导图并自动完成 MCP 预检查"
  default_prompt: "使用 $mindmap 前，先运行当前平台对应的 bootstrap 脚本；只有在脚本确认 markmap-mcp-server 已就绪后，才继续生成思维导图。"
policy:
  allow_implicit_invocation: false
dependencies:
  tools:
    - markmap-mcp-server
